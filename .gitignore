node_modules
dist
.env
repos
.zig-cache
zig-pkg
zig-out
.zig-version-cache

# Compiler binaries — package via scripts/build-compilers.mjs (CI/local only)
public/compilers/**
!public/compilers/.gitkeep
**/*.wasm
!**/node_modules/**/*.wasm

# AI coding tool artifacts (design docs, session plans, etc.)
.zcode/
x.ai/
